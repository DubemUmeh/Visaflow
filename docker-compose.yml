version: '3'

services:
  postgres:
    image: postgres:18.4.0
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: drizzle_turbo_db
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: